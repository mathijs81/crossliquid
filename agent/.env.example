# CrossLiquid Agent Configuration
# Copy this file to .env and fill in the values

# Environment mode: development, production, or testnet
# dev -> foundry only
# testnet -> {base, unichain}_sepolia
# production -> {base, optimism, mainnet, unichain}
ENVIRONMENT=development

# RPC Endpoints (use private endpoints for production)
RPC_FOUNDRY=
RPC_BASE=
RPC_OP_MAINNET=
RPC_ETHEREUM=
RPC_UNICHAIN=
RPC_BASE_SEPOLIA=
RPC_UNICHAIN_SEPOLIA=

# TODO: review all these other values, are they used, needed?

# Private key for vault transactions (funds management)
# WARNING: This key will have access to vault funds - use a dedicated safe/multisig in production
# Must be a valid hex string: 0x followed by 64 hex characters
VAULT_PRIVATE_KEY=0xac0974bec39a17e36ba4a6b4d238ff944bacb478cbed5efcae784d7bf4f2ff80

# LI.FI Configuration
LIFI_API_KEY=
LIFI_ROUTER_ADDRESS=

# Agent Configuration
AGENT_INTERVAL_MS=30000
LOG_LEVEL=info
NODE_ENV=development

# Server Configuration
PORT=3000
HOST=0.0.0.0

# Monitoring
ALERT_WEBHOOK_URL=

# Operator private key for CLI transactions (can reuse VAULT_PRIVATE_KEY or use separate key)
# For local testing, use one of Anvil's default accounts:
OPERATOR_PRIVATE_KEY=0xac0974bec39a17e36ba4a6b4d238ff944bacb478cbed5efcae784d7bf4f2ff80

# Chain ID for CLI operations (31337 for local Anvil, 8453 for Base, etc.)
CHAIN_ID=31337
