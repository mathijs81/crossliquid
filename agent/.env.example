# CrossLiquid Agent Configuration
# Copy this file to .env and fill in the values
# For production, use environment-specific files (.env.production, .env.staging)
# and ensure they are properly secured and never committed to version control

# Environment mode: development, production, or testnet
# Development mode disables chain initialization (for local Foundry setup)
# Production mode uses Base, Optimism, and other production chains
ENVIRONMENT=development

# RPC Endpoints (use private endpoints for production)
RPC_BASE=https://mainnet.base.org
RPC_OPTIMISM=https://mainnet.optimism.io
RPC_MAINNET=https://eth.llamarpc.com
RPC_INK=https://rpc.ink.network
RPC_UNICHAIN=https://rpc.unichain.network

# Private key for vault transactions (funds management)
# WARNING: This key will have access to vault funds - use a dedicated safe/multisig in production
# Must be a valid hex string: 0x followed by 64 hex characters
VAULT_PRIVATE_KEY=

# LI.FI Configuration
LIFI_API_KEY=
LIFI_ROUTER_ADDRESS=

# Agent Configuration
AGENT_INTERVAL_MS=30000
LOG_LEVEL=info
NODE_ENV=development

# Server Configuration
PORT=3000
HOST=0.0.0.0

# Monitoring
ALERT_WEBHOOK_URL=
